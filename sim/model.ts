// Kartenmodell für die Kampf-Sim — lebt jetzt in der App (einzige Quelle der
// Wahrheit), damit das In-App-Feature und das Konsolen-Werkzeug dieselbe Logik
// nutzen. Hier nur Re-Export.
export * from '../app/src/domain/sim/model';
